# Production Environment Variables for Vercel Deployment
# Copy these variables to Vercel Dashboard → Settings → Environment Variables

# ============================================================================
# Supabase Configuration (REQUIRED)
# ============================================================================
# Get these from: Supabase Dashboard → Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# API Configuration (REQUIRED if using external backend)
# ============================================================================

# Your backend API URL (Railway or other hosting)
NEXT_PUBLIC_API_URL=https://your-api-url.railway.app

# ============================================================================
# Optional: Analytics & Monitoring
# ============================================================================

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

# Google Analytics (if using)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================================
# Optional: Service Configuration
# ============================================================================

# TTS API (if direct integration)
# TTS_API_BASE_URL=https://your-tts-service.com

# ASR API (if direct integration)
# ASR_API_BASE_URL=https://your-asr-service.com

# ============================================================================
# Security (Server-side only - DO NOT prefix with NEXT_PUBLIC_)
# ============================================================================

# Supabase Service Role Key (for admin operations only)
# Only add this if you have server-side admin operations
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
# 
# 1. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 2. Variables without NEXT_PUBLIC_ are server-side only
# 3. Never commit actual values to Git
# 4. Add all variables in Vercel Dashboard before deployment
# 5. After adding variables, redeploy your project
# 
# To add variables in Vercel:
# 1. Go to your project → Settings → Environment Variables
# 2. Add each variable with its value
# 3. Select all environments (Production, Preview, Development)
# 4. Save and redeploy
# ============================================================================
